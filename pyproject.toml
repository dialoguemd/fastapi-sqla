[tool.poetry]
name = "fastapi-sqla"
version = "0.0.0"
description = "SqlAlchemy integration for FastAPI®"
authors = ["Hadrien David <hadrien.david@dialogue.co>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^6.0.2"
pytest-watch = "^4.2.0"
black = "^20.8b1"
pylama = "^7.7.1"
isort = "^5.5.3"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
